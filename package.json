{
  "name": "xloader-frontend-standalone",
  "version": "1.0.0",
  "description": "Standalone Frontend for XLoader",
  "main": "index.html",
  "scripts": {
    "dev": "browser-sync start --server \".\" --files \"*\" --port 8080",
    "dev:proxy": "browser-sync start --proxy \"localhost:28657\" --files \"*\" --port 8080",
    "build": "if exist dist (rd /s /q dist) && mkdir dist && xcopy /E /I assets dist\\assets && xcopy /E /I img dist\\img && copy *.json dist\\ && html-minifier --remove-comments --collapse-whitespace --minify-css true --minify-js true index.html -o dist/index.html"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "browser-sync": "^2.29.3",
    "html-minifier": "^4.0.0"
  }
}
